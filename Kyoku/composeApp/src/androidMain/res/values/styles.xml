<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Kyoku" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Base (API 26-30): core-splashscreen cannot animate an AVD here, so use a
         fully-drawn static icon. The animated variant lives in values-v31/styles.xml. -->
    <style name="Theme.Kyoku.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_static</item>
        <item name="postSplashScreenTheme">@style/Theme.Kyoku</item>
    </style>
</resources>
